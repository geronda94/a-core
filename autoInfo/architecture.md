📁 Проектируемая структура проектаPlaintexta-core/
├── main.py # Точка входа, основной цикл опроса сервера
├── config.json # Хранение ID устройства и настроек
├── identity.py # Генерация/чтение уникального ID
├── network/
│ ├── monitor.py # Определение IP, порта ADB, ротация
│ └── routing.py # Управление трафиком (мобильный интернет vs ADB)
├── core/
│ ├── adb_client.py # Низкоуровневая обертка над командами ADB
│ ├── actions.py # Классы для кликов, свайпов, ввода текста
│ └── vision.py # Скриншоты и парсинг XML-дерева
├── tasks/
│ ├── dispatcher.py # Разбор пакета команд от сервера
│ └── executor.py # Выполнение логики тасок
└── utils/
└── logger.py # Красивое логирование событий
Шаг 1: Идентификация и Сеть (Identity & Network)Первым делом нам нужно, чтобы телефон «представился» и сообщил свои технические параметры.1. Уникальный ID (UUID)При первом запуске скрипт генерирует случайный идентификатор и сохраняет его в config.json. Это позволит серверу привязать историю действий именно к этому железу.2. Определение параметровНам нужно собирать пакет данных для каждого запроса к серверу:External IP: Мобильный IP (через https://api.ipify.org).ADB Port: Текущий порт беспроводной отладки.Шаг 3: Объектно-ориентированная архитектура командВместо того чтобы писать os.system каждый раз, мы создадим классы. Это позволит делать сложные цепочки команд.Пример структуры классов:КлассОтветственностьКлючевые методыDeviceWatcherМониторинг состоянияget_adb_port(), get_mobile_ip()InputManagerФизические действияtap(x, y), swipe(x1, y1, x2, y2), type_text(str)ScreenAnalyzerЗрение ботаtake_screenshot(), find_text(target), get_xml_tree()TaskRunnerЛогика исполненияrun_package(json_list)Шаг 4: Изоляция трафика (ADB vs Internet)Это самый сложный технический момент. Чтобы трафик шел через мобильный интернет, а ADB работал локально:Wi-Fi (по которому идет ADB) должен быть без доступа к интернету (локальная точка доступа).В Android есть настройка в меню разработчика: «Не отключать мобильные данные» (Mobile data always active). Её нужно включить.В Python мы можем форсировать использование определенного интерфейса для запросов к серверу через библиотеку requests или системные маршруты (ip route).Реализация — Часть 1: Идентификация и СетьДавай начнем с базы. Создадим модуль, который подготовит телефон к работе.Файл identity.py (Генерация ID):Pythonimport uuid
import json
import os

CONFIG_PATH = "config.json"

def get_device_id():
if os.path.exists(CONFIG_PATH):
with open(CONFIG_PATH, 'r') as f:
return json.load(f).get("device_id")

    # Если файла нет, создаем новый ID
    new_id = str(uuid.uuid4())[:8] # Короткий ID для удобства
    with open(CONFIG_PATH, 'w') as f:
        json.dump({"device_id": new_id}, f)
    return new_id

Файл network/monitor.py (Сбор данных о сети):Pythonimport subprocess
import requests

def get_adb_port(): # Пробуем через системные свойства
try:
port = subprocess.check_output("getprop service.adb.tcp.port", shell=True).decode().strip()
if port and port != "-1":
return port
except:
pass
return "unknown"

def get_mobile_ip():
try: # Прямой запрос через мобильный интерфейс (если возможно)
return requests.get("https://api.ipify.org", timeout=5).text
except:
return "offline"
Шаг 5: Формат пакета команд (Task Packet)Сервер должен отдавать не просто одну команду, а список. Это минимизирует количество запросов.Пример JSON от сервера:JSON{
"task_id": "12345",
"commands": [
{"action": "wait", "value": 2},
{"action": "tap", "x": 500, "y": 1200},
{"action": "type", "text": "Hello world"},
{"action": "screenshot", "upload": true},
{"action": "rotate_ip", "delay": 5}
]
}
Как мы будем действовать дальше?Я предлагаю собрать проект по частям.Сначала мы напишем core/adb_client.py — это будет мощный класс, который берет на себя все ошибки ADB, проверки подключения и выполнение команд.Затем создадим tasks/dispatcher.py, который будет «пережевывать» JSON от сервера и вызывать нужные классы.В конце объединим всё в main.py с бесконечным циклом и защитой от падений.
